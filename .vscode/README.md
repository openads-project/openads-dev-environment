# vscode

Shared VS Code configuration for ROS 2 module repositories.

This folder is managed via Copier from `OpenAutomatedDriving/dev-environment`.
To update managed files, run `copier update` in the module repository root.

Local, module-specific changes should go into files that are not template-managed
or into dedicated `*.local` files.
