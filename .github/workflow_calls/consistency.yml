name: consistency

on:
  push:
  workflow_dispatch:

jobs:
  consistency:
    uses: openads-project/openads-dev-environment/.github/workflows/consistency.yml@consistency-forks
    with:
      skip-checks: {{ skipped_check_ids }}
    secrets: inherit
