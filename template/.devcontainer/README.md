# devcontainer

Shared devcontainer definition for ROS 2 module repositories.

This folder is managed via Copier from `OpenAutomatedDriving/dev-environment`.
To update managed files, run `copier update` in the module repository root.

You can override the image selection by setting `VSCODE_DEVCONTAINER_IMAGE` in
an `.envrc` file in the module repository root.
